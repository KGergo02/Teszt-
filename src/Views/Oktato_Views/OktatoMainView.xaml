<Page x:Class="Teszt__.src.Views.Oktato_Views.OktatoMainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Teszt__.src.Views.Oktato_Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      KeepAlive="True"
      Title="{Binding TitleName}" Background="#FFFF7400">

    <ScrollViewer VerticalScrollBarVisibility="Hidden">
        <Viewbox VerticalAlignment="Top">
            <StackPanel>
                <Menu FontWeight="Bold">
                    <MenuItem Header="Kurzus létrehozása" Command="{Binding CreateCourseCommand}"/>
                    <MenuItem Header="Teszt létrehozása" Command="{Binding CreateTestCommand}"/>
                    <MenuItem Header="Kérdés létrehozása" Command="{Binding CreateQuestionCommand}"/>
                    <MenuItem Header="Eredmények megtekintése" Margin="0,0,13,0"/>
                    <MenuItem Header="Profil">
                        <MenuItem Header="Adatok módosítása" FontSize="10"/>
                        <MenuItem Header="Kijelentkezés" FontSize="10" Command="{Binding LogOutCommand}"/>
                    </MenuItem>
                </Menu>

                <StackPanel x:Name="mainStackPanel" Margin="0,28,0,0" HorizontalAlignment="Center" VerticalAlignment="Center">

                </StackPanel>

                <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,10,0,0">
                    <Button Content="Új sor" Width="50" Command="{Binding AddNewRowCommand}" Margin="0,0,16,0"/>
                    <Button Content="Küldés" Width="50" Command="{Binding SendCommand}"/>
                </WrapPanel>
            </StackPanel>
        
        </Viewbox>
    </ScrollViewer>
</Page>
